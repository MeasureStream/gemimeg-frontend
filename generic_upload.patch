Subject: [PATCH] Task/generic upload
---
Index: src/app/app.module.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/app.module.ts b/src/app/app.module.ts
--- a/src/app/app.module.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/app.module.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -81,7 +81,6 @@
 import { CasDeviceComponent } from './components/cas/cas-device/cas-device.component';
 import { MathModule } from './mathml/math/math.module';
 
-
 import { DccRichcontentComponent } from './components/dcc/dcc-richcontent/dcc-richcontent.component';
 import { DccByteDataComponent } from './components/dcc/dcc-byte-data/dcc-byte-data.component';
  import { InfoButtonComponent } from './components/common/info-button/info-button.component';
Index: src/app/components/common/generic-file-upload/file-upload.service.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/common/generic-file-upload/file-upload.service.ts b/src/app/components/common/generic-file-upload/file-upload.service.ts
new file mode 100644
--- /dev/null	(revision bf32f4c424651eefc3428dca813d107414dff669)
+++ b/src/app/components/common/generic-file-upload/file-upload.service.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -0,0 +1,27 @@
+import { Observable } from 'rxjs';
+import { Injectable } from '@angular/core';
+import { HttpClient, HttpHeaders } from '@angular/common/http';
+import { ByteDataDto } from 'src/app/generated/dcc/model/byteDataDto';
+
+@Injectable({
+  providedIn: 'root',
+})
+export class FileUploadService {
+  private finalEndpoint = '/api/v1/cache';
+
+  constructor(private http: HttpClient) {}
+
+  uploadFile(fileData: ByteDataDto, cacheEnabled: boolean): Observable<any> {
+    const headers = new HttpHeaders().set('Content-Type', 'application/json; charset=utf-8');
+    const options: Object = {
+      headers,
+      observe: 'response',
+      responseType: 'json',
+    };
+
+    return this.http.post<any>(this.finalEndpoint, fileData, options);
+  }
+  retrieveFile(link: string): Observable<any> {
+    return this.http.get<any>(link, { responseType: 'text' as 'json' });
+  }
+}
Index: src/app/components/common/generic-file-upload/generic-file-upload.component.html
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/common/generic-file-upload/generic-file-upload.component.html b/src/app/components/common/generic-file-upload/generic-file-upload.component.html
--- a/src/app/components/common/generic-file-upload/generic-file-upload.component.html	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/common/generic-file-upload/generic-file-upload.component.html	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -26,7 +26,7 @@
 <div class="row">
   <div class="col-xs-12">
     <div *ngIf="backendResponse" class="selected-file">
-      <h3>Gesiegelte Datei: {{ backendResponse.body.fileName }}</h3>
+      <h3>Gesiegelte Datei: {{ backendResponse.fileName }}</h3>
     </div>
   </div>
 </div>
Index: src/app/components/common/generic-file-upload/generic-file-upload.component.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/common/generic-file-upload/generic-file-upload.component.ts b/src/app/components/common/generic-file-upload/generic-file-upload.component.ts
--- a/src/app/components/common/generic-file-upload/generic-file-upload.component.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/common/generic-file-upload/generic-file-upload.component.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -1,3 +1,4 @@
+import { FileUploadService } from './file-upload.service';
 import { Subject } from 'rxjs';
 import { Component, OnInit, Output } from '@angular/core';
 import { DigiSealService } from 'src/app/services/digi-seal/digi-seal.service';
@@ -9,18 +10,17 @@
   templateUrl: './generic-file-upload.component.html',
   styleUrls: ['./generic-file-upload.component.scss'],
 })
-
 export class GenericFileUploadComponent implements OnInit {
   files: any = {};
   backendResponse: any;
-  selectedFile: File | null = null; // New property to store the selected file
+  selectedFile: File | null = null; 
   @Output() fileUpload = new Subject<File>();
   fileData: ByteDataDto = {};
+  cacheEnabled = true;
+  response: any;
+  binary_string: any;
 
-  constructor(
-    private digiSealService: DigiSealService,
-    private _snackBar: MatSnackBar
-  ) {}
+  constructor(private digiSealService: DigiSealService, private fileUploadService: FileUploadService, private _snackBar: MatSnackBar) {}
 
   ngOnInit() {}
 
@@ -33,7 +33,7 @@
         alert('File size exceeds the limit. Please choose a smaller file.');
         return;
       }
-      this.selectedFile = file; // Store the selected file
+      this.selectedFile = file;
     }
   }
 
@@ -51,22 +51,18 @@
     reader.onload = () => {
       const arrayBuffer = reader.result as ArrayBuffer;
       const uint8Array = new Uint8Array(arrayBuffer);
-      const binaryString = String.fromCharCode.apply(
-        null,
-        Array.from(uint8Array)
-      );
+      const binaryString = String.fromCharCode.apply(null, Array.from(uint8Array));
 
       const base64String = window.btoa(binaryString);
+
       this.files.mimeType = file.type;
       this.files.fileName = file.name;
-      this.files.content = [base64String];
-      this.files.embedded = false;
-      this.digiSealService.signPdfEmbedded(this.files).subscribe({
+      this.files.fileContent = base64String;
+      //this.files.embedded = false;
+      this.fileUploadService.uploadFile(this.files, this.cacheEnabled).subscribe({
         next: (res) => {
           this.backendResponse = res;
-          console.log("backend Response",this.backendResponse);
           this.showSuccessToast();
-          
         },
         error: (err: any) => {
           this.showErrorToast('Error signing/uploading the file. Please try again.');
@@ -75,54 +71,45 @@
     };
     reader.readAsArrayBuffer(file);
   }
-
+  
   onFileDownload() {
-    // Get the response payload
-    let sealedResponse = this.backendResponse.body.payload;
-  
-    // Strip off any Base64 data URL prefix if present
-    //sealedResponse = sealedResponse.split(',')[1] || sealedResponse;
-  
-    // Decode the Base64 string
-    console.log("sealed Response2",sealedResponse);
-    
-    let binary_string = '';
-    try {
-      binary_string = window.atob(sealedResponse);
-    } catch (e:any) {
-      console.error('Invalid Base64 string:', e.message);
-      return;
-    }
-  
-    // Convert binary string to ArrayBuffer
-    const len = binary_string.length;
-    const arrayBuffer = new ArrayBuffer(len);
-    const bytes = new Uint8Array(arrayBuffer);
-    for (let i = 0; i < len; i++) {
-      bytes[i] = binary_string.charCodeAt(i);
-    }
-  
-    // Create a blob with the specified MIME type
-    const blob = new Blob([bytes], {
-      type: this.backendResponse.body.mimeType,
-    });
-  
-    // Create a temporary download link
-    let a = document.createElement('a');
-    document.body.appendChild(a);
-    const url = window.URL.createObjectURL(blob);
-    a.href = url;
-    a.download = this.backendResponse.body.fileName;
-    a.click();
-  
-    // Clean up
-    window.URL.revokeObjectURL(url);
-    a.remove();
-  
-    // Optionally, show a download notification
-    this.showDownloadToast();
+    const fullUrl = this.backendResponse.body.retrievalUrl;
+
+
+    this.fileUploadService.retrieveFile(fullUrl).subscribe({
+      next: (res) => {
+        let parsedJSON = JSON.parse(res);
+        this.backendResponse = parsedJSON;
+
+
+        this.binary_string = atob(this.backendResponse.fileContent);
+
+        const len = this.binary_string.length;
+        const arrayBuffer = new ArrayBuffer(len);
+        const bytes = new Uint8Array(arrayBuffer);
+        for (let i = 0; i < len; i++) {
+          bytes[i] = this.binary_string.charCodeAt(i);
+        }
+        //Blobbinary large objects
+        const blob = new Blob([bytes], { type: this.backendResponse.mimeType });
+
+        let a = document.createElement('a');
+        document.body.appendChild(a);
+        const url = window.URL.createObjectURL(blob);
+        a.href = url;
+        a.download = this.backendResponse.fileName;
+        a.click();
+
+        window.URL.revokeObjectURL(url);
+        a.remove();
+
+        this.showDownloadToast();
+      },
+      error: (err: any) => {
+        this.showErrorToast('Error downloading the file. Please try again.');
+      },
+    });
   }
-  
 
   private showSuccessToast(): void {
     this._snackBar.open('File signed and uploaded successfully!', 'Close', {
@@ -152,7 +139,6 @@
   }
 }
 
-
 /* filename: string;
 customText: string;
 file: File | undefined;
Index: src/app/components/dcc/dcc-measurement-metadata/dcc-measurement-metadata.component.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/dcc/dcc-measurement-metadata/dcc-measurement-metadata.component.ts b/src/app/components/dcc/dcc-measurement-metadata/dcc-measurement-metadata.component.ts
--- a/src/app/components/dcc/dcc-measurement-metadata/dcc-measurement-metadata.component.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/dcc/dcc-measurement-metadata/dcc-measurement-metadata.component.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -14,21 +14,24 @@
   selector: 'app-dcc-measurement-metadata',
   templateUrl: './dcc-measurement-metadata.component.html',
   styleUrls: ['./dcc-measurement-metadata.component.scss'],
-
 })
 export class DccMeasurementMetadataComponent implements OnInit {
 
   @Input() list: Array<StatementDto>;
   @Input() header: string;
 
-
+  item = { date: '' };
+  statementDate:Date|any= "2022-09-09"
+  startDate = new Date(2024, 1, 1)
   validConformityStatementStatusTypes = ["pass", "fail", "conditionalPass", "conditionalFail", "noPass", "noFail"];
 
   isExpanded: boolean[] = [true];
 
-  constructor(public dccService: DccService) {
+  constructor(  public dccService:DccService) {
     this.list = new Array<StatementDto>;
     this.header = "";
+   
+   
   }
 
   ngOnInit(): void {
@@ -98,9 +101,29 @@
         (statement[fieldName] as string[]) = processedArray.filter(code => code !== '');
       }
     }
-
+  
   }
 
+
+
+
+  // Method to format the date to 'YYYY-MM-DD'
+  
+  marshalCustomDate(value: Date): string {
+    const result = new Array<string>();
+    if (value) {
+      result.push(value.getFullYear().toString());
+      result.push((value.getMonth() + 1).toString());
+      result.push(value.getDate().toString());
+    }
+    // Append leading zeros for single-digit month and day
+    for (let i = 0; i < result.length; i++) {
+      if (result[i].length === 1) {
+        result[i] = '0' + result[i];
+      }
+    }
+    return result[0] + '-' + result[1] + '-' + result[2];}
+
   toggleCard(index: number) {
     this.isExpanded[index] = !this.isExpanded[index];
   }
@@ -108,4 +131,9 @@
     this.isExpanded.push(true);
   }
 
+  // Handles the date change event
+  onDateChange(event: any) {
+    const date: Date = event.value;
+    this.item.date = this.marshalCustomDate(date); // Format the date immediately
+  }
 }
Index: src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.html
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.html b/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.html
--- a/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.html	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.html	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -43,7 +43,7 @@
   </div>
    -->
 
-<mat-toolbar color="light">
+<mat-toolbar color="normal">
   <div class="icon-container">
     <button (click)="toggleComponent('language')" mat-icon-button matTooltip="Select Language">
       <mat-icon>language</mat-icon>
Index: src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.scss
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.scss b/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.scss
--- a/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.scss	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.scss	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -17,7 +17,7 @@
     margin-bottom: 16px;
   }
   .icon-container button:hover mat-icon {
-    color: #4a55ec;
+    color: #b1b2c2;
   }
   .elements{
     margin-top: 10px;
Index: src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.ts b/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.ts
--- a/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/dcc/dcc-richcontent/dcc-richcontent.component.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -73,4 +73,7 @@
     var result = <LangTextPair>{};
     return result;
   }
+  onFileUploading(file:File){
+    console.log('file uploaded', file);
+  }
 }
Index: src/app/components/digi-seal/digi-seal.component.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/digi-seal/digi-seal.component.ts b/src/app/components/digi-seal/digi-seal.component.ts
--- a/src/app/components/digi-seal/digi-seal.component.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/digi-seal/digi-seal.component.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -1,4 +1,7 @@
+import { ByteDataDto } from './../../generated/dcc/model/byteDataDto';
 import { Component, OnInit } from '@angular/core';
+import { FileUploadService } from '../common/generic-file-upload/file-upload.service';
+import { DigiSealService } from 'src/app/services/digi-seal/digi-seal.service';
 
 @Component({
   selector: 'app-digi-seal',
@@ -7,11 +10,15 @@
 })
 export class DigiSealComponent implements OnInit {
   files: any = {};
+  fileData:ByteDataDto|any
+ private basePath = 'api/v1/dseal/test'
+  constructor(private digiSeal:DigiSealService) {}
 
-  constructor() {}
-
-  onUploadFile(file: any) {
-    console.log('file uploaded', file);
+  onUploadFile(file: File) {
+    this.digiSeal.signPdfEmbedded(this.fileData, this.basePath).subscribe(response => {
+      console.log('File uploaded to generic storage:', response);
+      console.log('file uploaded', file);
+    });
   }
 
   ngOnInit() {}
Index: src/app/components/efile/procedure-detail/procedure-detail.component.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/components/efile/procedure-detail/procedure-detail.component.ts b/src/app/components/efile/procedure-detail/procedure-detail.component.ts
--- a/src/app/components/efile/procedure-detail/procedure-detail.component.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/components/efile/procedure-detail/procedure-detail.component.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -25,6 +25,7 @@
   public documents: DocumentDto[] = [];
   private id: string = "";
 byteData: ByteDataDto|any
+baseUrl:string|any
   constructor(public ObjectsService: ObjectsService, private route: ActivatedRoute, public dialog: MatDialog, private dsealService: DigiSealService, private logger: NGXLogger, private errorService: ErrorService) {
     this.route.params.subscribe(params => this.id = params["id"]);
   }
@@ -64,7 +65,7 @@
           let signedFileData: Blob;
           let responseData: any;
           if (record.recordType == 'PDF') {
-            this.dsealService.signPdfEmbedded(this.byteData).subscribe({
+            this.dsealService.signPdfEmbedded(this.byteData, this.baseUrl).subscribe({
               next: (response: HttpResponse<any>) => {
                 responseData = response;
                 this.uploadNewRecord(document,record,response.body);
@@ -75,7 +76,7 @@
             });
           } 
           else {
-            this.dsealService.signDetached(response).subscribe({
+            this.dsealService.signDetached(response, this.baseUrl).subscribe({
               next: (response: HttpResponse<any>) => {
                 responseData = response;
                 this.uploadNewRecord(document,record,response.body);
Index: src/app/services/dcc/dcc.service.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/services/dcc/dcc.service.ts b/src/app/services/dcc/dcc.service.ts
--- a/src/app/services/dcc/dcc.service.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/services/dcc/dcc.service.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -95,3 +95,5 @@
 
   }
 }
+
+
Index: src/app/services/digi-seal/digi-seal.service.ts
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/app/services/digi-seal/digi-seal.service.ts b/src/app/services/digi-seal/digi-seal.service.ts
--- a/src/app/services/digi-seal/digi-seal.service.ts	(revision 8faf983abbdb2ec8ea0a25f28de84ab72402df55)
+++ b/src/app/services/digi-seal/digi-seal.service.ts	(revision bf32f4c424651eefc3428dca813d107414dff669)
@@ -9,14 +9,13 @@
   providedIn: 'root'
 })
 export class DigiSealService {
-  private dsealServicePath = 'api/v1/dseal/test'; // TODO: When digiSeal Server is working again, remove '/test'!
-  fileContent = ' ';
-  base64Content = this.fileContent.split(',')[1];
+  /* private basePath = 'api/v1/dseal/test';  */// TODO: When digiSeal Server is working again, remove '/test'!
+
   backendResponseJson: any;
-
+  
   constructor(private http: HttpClient, private logger: NGXLogger) {}
-
-  signPdfEmbedded(fileData: ByteDataDto): Observable<any> {
+  
+  signPdfEmbedded(fileData: ByteDataDto, basePath:string ): Observable<any> {
     let headers = new HttpHeaders();
     headers = headers.set('Content-Type', 'application/json; charset=utf-8');
     const options: Object = {
@@ -24,10 +23,10 @@
       observe: 'response',
       responseType: 'json',
     };
-    return this.http.post<any>(this.dsealServicePath, fileData, options);
+    return this.http.post<any>(basePath, fileData, options);
   }
 
-  signDetached(fileData: ArrayBuffer) {
+  signDetached(fileData: ArrayBuffer, basePath:string) {
     let headers = new HttpHeaders();
     headers = headers.set('Content-Type', 'application/octet-stream');
     headers = headers.set('Accept', '/*');
@@ -36,6 +35,7 @@
       observe: 'response',
       responseType: 'json',
     };
-    return this.http.post<any>(this.dsealServicePath, fileData, options);
+
+    return this.http.post<any>(basePath, fileData, options);
   }
 }
